```sh
git init # 初始化git仓库
git add README.md # 将 工作区的 README.md 文件添加到暂存区
git commit -m "first commit" # 提交暂存区的文件到本地仓库
git branch -M main # 创建分支
git remote add origin git@github.com:lxm520hack/cms_fastapi.git # 添加远程仓库
git push -u origin main # 本地仓库推送到远程仓库
```

